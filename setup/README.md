# Setup

Repository tooling — not skills. Each subfolder is a self-contained module you install once, with its own `install.sh`.

- **[memory](./memory/README.md)** — Symlinks each project's auto-memory folder to `./memory` (git-ignored) and installs the memory-capture protocol.

Install everything:

```bash
bash setup/install.sh
```
